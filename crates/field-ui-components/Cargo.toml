[package]
name = "field-ui-components"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Reusable GPUI chrome, waveform display, and host data traits for FieldAssist"

[dependencies]
gpui-kit = { workspace = true }
