[package]
name = "field-ui-components"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Reusable GPUI chrome and waveform provider trait for FieldAssist"

[dependencies]
gpui-kit = { workspace = true }
